#include "main.h"

/**
 * add - adds two integers and returns the result
 * @t: parameter
 * @d: parameter
 * Return: returns the addition of two integers
 */

int add(int t, int d)
{
	return (t + d);
}
